cleari()
ArrayBuffer.empty[Int]

repeatFor(ArrayBuffer.empty) { a =>
  println(a)
}
HashMap.empty[String, Int]

repeatFor(HashMap.empty) { a =>
  println(a)
}


HashSet.empty[Picture]

repeatFor(HashSet.empty) { a =>
  println(a)
}

println(ArrayBuffer.empty.contains(Int))

println(HashMap.empty.contains(Int))

println(HashSet.empty.contains(Int))
